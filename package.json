{
  "name": "mood-mapper",
  "version": "1.0.0",
  "description": "Mood Mapper",
  "main": "server.js",
  "scripts": {
    "server": "node server.js",
    "client": "node scripts/start-client.js",
    "start": "concurrently \"nodemon server.js\" \"npm run client\"",
    "build": "node scripts/build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.5.0",
    "nodemon": "^1.17.3"
  },
  "dependencies": {
    "dotenv": "^5.0.1",
    "express": "^4.15.4",
    "mysql2": "^1.5.3",
    "path": "^0.12.7",
    "sequelize": "^4.37.6"
  }
}
